--- extracted/sglang/bench_serving.py	2026-04-26 06:37:01.563985410 +0000
+++ /workspace/peek/lib/python3.12/site-packages/sglang/bench_serving.py	2026-04-26 00:24:14.909609262 +0000
@@ -1105,6 +1105,18 @@
     if not input_requests:
         return
 
+    # === PEEK PATCH: cross-session shared system prompt ============
+    # Loads a static N-token system prompt that gets prepended to every
+    # session. Models the "tool definitions / agent instructions" prefix
+    # that real agentic systems share across all sessions (Cursor, Copilot,
+    # Claude Code, RAG). Activated by PEEK_SHARED_SYSTEM_PROMPT_PATH.
+    _peek_shared_sys_path = os.environ.get("PEEK_SHARED_SYSTEM_PROMPT_PATH", "")
+    _peek_shared_sys_text = ""
+    if _peek_shared_sys_path and os.path.exists(_peek_shared_sys_path):
+        with open(_peek_shared_sys_path) as _f:
+            _peek_shared_sys_text = _f.read()
+    # === /PEEK PATCH ===========================================
+
     input_requests.sort(key=lambda r: r["timestamp"])
 
     start_time = time.perf_counter()
@@ -1134,6 +1146,10 @@
 
         output_len_per_round = record.get("output_length", 256)
         chat_history = []
+        # === PEEK PATCH: prepend shared system prompt if loaded ==========
+        if _peek_shared_sys_text:
+            chat_history.append({"role": "system", "content": _peek_shared_sys_text})
+        # === /PEEK PATCH ===============================================
 
         for i in range(num_rounds):
             # Add user query for the current round
@@ -1167,6 +1183,19 @@
             placeholder_response = " ".join(["story"] * output_len_per_round)
             chat_history.append({"role": "assistant", "content": placeholder_response})
 
+            # === PEEK PATCH: tight tool-chain inter-turn gap ============
+            # Models agentic burst patterns (Cursor / Copilot / Claude Code
+            # tool chains, RAG pipelines). When PEEK_AGENT_INTER_TURN_MEDIAN_MS
+            # is unset or 0, behavior is identical to upstream sglang.
+            _peek_gap_med_ms = float(os.environ.get("PEEK_AGENT_INTER_TURN_MEDIAN_MS", "0"))
+            if _peek_gap_med_ms > 0 and i < num_rounds - 1:
+                import math
+                _peek_gap_sigma = float(os.environ.get("PEEK_AGENT_INTER_TURN_SIGMA", "0.5"))
+                _peek_mu = math.log(_peek_gap_med_ms / 1000.0)
+                _peek_gap_s = random.lognormvariate(_peek_mu, _peek_gap_sigma)
+                await asyncio.sleep(_peek_gap_s)
+            # === /PEEK PATCH ===========================================
+
 
 def sample_mmmu_requests(
     num_requests: int,
@@ -2326,14 +2355,19 @@
     # Check for multi-turn: prompt is a list of strings (not OpenAI messages dicts)
     # Multi-turn format: ["turn1", "turn2", ...] - list of strings
     # OpenAI format: [{"role": "user", "content": "..."}, ...] - list of dicts
-    first_prompt = input_requests[0].prompt
-    is_multi_turn = (
-        isinstance(first_prompt, list)
-        and len(first_prompt) > 0
-        and isinstance(first_prompt[0], str)
-    )
-    if is_multi_turn:
-        request_func = wrap_multi_turn_request_func(request_func, backend=backend)
+    # NOTE: Mooncake mode uses raw dicts and handles multi-turn via its own
+    # async generator (`get_mooncake_request_over_time`), so skip this check.
+    if args.dataset_name == "mooncake":
+        is_multi_turn = False
+    else:
+        first_prompt = input_requests[0].prompt
+        is_multi_turn = (
+            isinstance(first_prompt, list)
+            and len(first_prompt) > 0
+            and isinstance(first_prompt[0], str)
+        )
+        if is_multi_turn:
+            request_func = wrap_multi_turn_request_func(request_func, backend=backend)
 
     # Limit concurrency
     # From https://github.com/vllm-project/vllm/pull/9390
@@ -2556,7 +2590,7 @@
     # Compute metrics and print results
     benchmark_duration = time.perf_counter() - benchmark_start_time
     metrics, output_lens = calculate_metrics(
-        input_requests=None if is_multi_turn else input_requests,
+        input_requests=None if (is_multi_turn or args.dataset_name == "mooncake") else input_requests,
         outputs=outputs,
         dur_s=benchmark_duration,
         tokenizer=tokenizer,
